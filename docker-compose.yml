networks:
  rs-network:
    driver: bridge
services:
  postgres:
    container_name: engicad-postgres
    image: postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: engicad
    networks:
      - rs-network